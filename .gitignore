node_modules
.expo
dist
web-build
coverage
.env
*.log
.DS_Store
ios
android

# Maestro artifacts
.maestro/tests
.maestro/output

# Claude Code's own scratch space (e.g. isolated agent worktrees) — never repo content.
.claude/
